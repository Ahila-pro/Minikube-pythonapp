apiVersion: v1
kind: Service
metadata:
  name: {{ include "minikube-pythonapp.fullname" . }}
  labels:
    {{- include "minikube-pythonapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minikube-pythonapp.selectorLabels" . | nindent 4 }}
