apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minikube-pythonapp.fullname" . }}-test-connection"
  labels:
    {{- include "minikube-pythonapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minikube-pythonapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
